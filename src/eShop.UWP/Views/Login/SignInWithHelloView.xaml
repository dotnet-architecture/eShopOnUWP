<UserControl
    x:Class="eShop.UWP.Views.SignInWithHelloView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:controls="using:eShop.UWP.Controls"
    mc:Ignorable="d" d:DesignWidth="640" d:DesignHeight="320">

    <StackPanel>
        <Grid HorizontalAlignment="Center">
            <TextBlock Text="{Binding UserName}" HorizontalAlignment="Center" FontSize="22" />
        </Grid>
        <controls:ButtonControl IconSourcePath="{StaticResource HelloIcon}" Style="{StaticResource HelloButtonStyle}" Command="{Binding LoginWithHelloCommand}" Foreground="Black" UseSystemFocusVisuals="False" />
    </StackPanel>

</UserControl>
